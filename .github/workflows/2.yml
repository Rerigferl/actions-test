name: "2"

on:
  workflow_dispatch:

jobs:
  job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build
        uses: gomorroth/vpm-build-repository@2739a2f79ced8f46c6958110d8564fc518f751bd
        with:
          repo-token: ${{ github.token }}

      - run: |
          ls -l
        
