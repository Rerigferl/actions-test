name: "2"

on:
  workflow_dispatch:

permissions:
  contents: write
  id-token: write

jobs:
  job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build
        uses: gomorroth/vpm-build-repository@74687ac8ddb9182cc3c00140c8032d682cd35a8d
        with:
          repo-token: ${{ github.token }}

      - run: |
          ls -l
        
