name: "2"

on:
  workflow_dispatch:

jobs:
  job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build
        uses: gomorroth/vpm-build-repository@8525bcb3650dc6457ab546940c5788db676476d0
        with:
          repo-token: ${{ github.token }}

      - run: |
          ls -l
        
