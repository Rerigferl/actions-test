name: "2"

on:
  workflow_dispatch:

jobs:
  job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build
        uses: gomorroth/vpm-build-repository@6cee385534d78c716cf6f3fcce9ae76d00563268
        with:
          repo-token: ${{ github.token }}

      - run: |
          ls -l
        
